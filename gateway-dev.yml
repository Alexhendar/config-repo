spring:
  cloud:
    gateway:
      discovery:
        locator:
          enabled: true
      default-filters:
      - AddResponseHeader=X-Response-Default-Foo, Default-Bar
      routes:
      - id: service_to_user
        uri: lb://user-service
        order: 8000
        predicates: 
        - Path=/user/**
        filters:
        - StripPrefix=1
